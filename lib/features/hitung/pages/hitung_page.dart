import 'package:flutter/material.dart';

class HitungPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Hitung")),
      body: Center(child: Text("Halaman Hitung")),
    );
  }
}